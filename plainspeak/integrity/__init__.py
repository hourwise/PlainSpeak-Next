"""Protecting meaning from the transformation engine.

Today this is the register of domain terms of art that must never be
substituted. It is the foundation for the wider integrity layer — numbers,
dates, negation, modal verbs, identifiers — described in the build plan.
"""


