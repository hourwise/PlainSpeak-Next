# 6 terms the inherited glossary would substitute, and this engine
# will not.
#
# Each is worth flagging and cannot be replaced mechanically: a word with two
# senses, a gloss standing in for a definition, or a replacement the integrity
# firewall would veto. The reason travels with the rule, because a reader told
# only that a word is complex has been given nothing to act on.
#
# Generated by tools/build_glossary_inventory.py from migration/decisions.yaml.

id: PS.AMBIGUOUS.031
version: 1
name: subsequent-is-ambiguous
mode: diagnostic
description: >
  The inherited glossary suggested replacing "subsequent" with
  "later". This engine reports it instead: 'later' shifts an ordering the firewall protects.
match:
  type: word
  text: "subsequent"
scope:
  include: [prose]
priority: 70
reason:
  short: "'later' shifts an ordering the firewall protects"
provenance:
  source: "PlainSpeak inherited glossary, reviewed and kept as a diagnostic"
  reference: "plainspeak/core/glossary.py"
  licence: "project-authored"
examples:
  positive:
    - "The notice used the word subsequent here."
  negative:
    - "A different word entirely appears instead."

---
id: PS.AMBIGUOUS.032
version: 1
name: substantial-is-ambiguous
mode: diagnostic
description: >
  The inherited glossary suggested replacing "substantial" with
  "large". This engine reports it instead: carries a legal threshold in some documents.
match:
  type: word
  text: "substantial"
scope:
  include: [prose]
priority: 70
reason:
  short: "carries a legal threshold in some documents"
provenance:
  source: "PlainSpeak inherited glossary, reviewed and kept as a diagnostic"
  reference: "plainspeak/core/glossary.py"
  licence: "project-authored"
examples:
  positive:
    - "The notice used the word substantial here."
  negative:
    - "A different word entirely appears instead."

---
id: PS.AMBIGUOUS.033
version: 1
name: sustain-is-ambiguous
mode: diagnostic
description: >
  The inherited glossary suggested replacing "sustain" with
  "support". This engine reports it instead: means both 'support' and 'suffer' (an injury).
match:
  type: word
  text: "sustain"
scope:
  include: [prose]
priority: 70
reason:
  short: "means both 'support' and 'suffer' (an injury)"
provenance:
  source: "PlainSpeak inherited glossary, reviewed and kept as a diagnostic"
  reference: "plainspeak/core/glossary.py"
  licence: "project-authored"
examples:
  positive:
    - "The notice used the word sustain here."
  negative:
    - "A different word entirely appears instead."

---
id: PS.AMBIGUOUS.034
version: 1
name: terminate-is-ambiguous
mode: diagnostic
description: >
  The inherited glossary suggested replacing "terminate" with
  "end". This engine reports it instead: a settled sense in contract and employment law.
match:
  type: word
  text: "terminate"
scope:
  include: [prose]
priority: 70
reason:
  short: "a settled sense in contract and employment law"
provenance:
  source: "PlainSpeak inherited glossary, reviewed and kept as a diagnostic"
  reference: "plainspeak/core/glossary.py"
  licence: "project-authored"
examples:
  positive:
    - "The notice used the word terminate here."
  negative:
    - "A different word entirely appears instead."

---
id: PS.AMBIGUOUS.035
version: 1
name: witnessed-is-ambiguous
mode: diagnostic
description: >
  The inherited glossary suggested replacing "witnessed" with
  "saw". This engine reports it instead: 'saw' loses the formal sense of attesting.
match:
  type: word
  text: "witnessed"
scope:
  include: [prose]
priority: 70
reason:
  short: "'saw' loses the formal sense of attesting"
provenance:
  source: "PlainSpeak inherited glossary, reviewed and kept as a diagnostic"
  reference: "plainspeak/core/glossary.py"
  licence: "project-authored"
examples:
  positive:
    - "The notice used the word witnessed here."
  negative:
    - "A different word entirely appears instead."

---
id: PS.AMBIGUOUS.036
version: 1
name: yield-is-ambiguous
mode: diagnostic
description: >
  The inherited glossary suggested replacing "yield" with
  "return". This engine reports it instead: means both 'give way' and 'financial return'.
match:
  type: word
  text: "yield"
scope:
  include: [prose]
priority: 70
reason:
  short: "means both 'give way' and 'financial return'"
provenance:
  source: "PlainSpeak inherited glossary, reviewed and kept as a diagnostic"
  reference: "plainspeak/core/glossary.py"
  licence: "project-authored"
examples:
  positive:
    - "The notice used the word yield here."
  negative:
    - "A different word entirely appears instead."
